The Array Object enables storing a collection of multiple items under a single variable name, and has members for performing common array operations.

Concept,What you need to know,Why use it?

Immutability,Don't change the original array.,Prevents weird bugs in large apps.

Spread ...,"""Unpacks"" the array into a new one.",Easiest way to clone or merge arrays.

.reduce(),Turns an array into one thing (number/object).,"Perfect for totals, averages, or tallies."

Chaining,Connecting .filter().map().find().,Allows complex data processing in 3 lines of code.

Method,Changes Original?,Best Use Case

slice(), No, "Getting a ""preview"" or a sub-section of a list."

splice(), Yes, "Removing a specific item by index or ""swapping"" items."

concat(), No, Joining two separate lists together.

reduce(), No, "Calculating totals, averages, or building a summary object."
